# Build
build/
build-release/

# macOS
.DS_Store

# clangd (symlink to build/compile_commands.json)
compile_commands.json

# Local secrets (TMDB / IGDB API keys) — never commit
.env

# IDE / editor tooling (local only — not product source)
.vscode/
.idea/
.cache/
*.swp
*~

# Compiled
*.o
*.so
*.a

# Internal docs / notes (not for the public repo) — drop anything in here
/internal/

# local test build dir
build-tests/
build-asan/

# fork-from-source build dir
build-fork/

# dev-only Qt ffmpeg media plugin (built locally, see internal/REVIEW.md W5)
/dev-qt-plugins/
